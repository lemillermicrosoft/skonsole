{
  "schema": 1,
  "type": "completion",
  "description": "Generate a commit message based on a git diff file output.",
  "completion": {
    "max_tokens": 1024,
    "temperature": 0.0,
    "top_p": 0.0,
    "presence_penalty": 0.0,
    "frequency_penalty": 0.0
  },
  "input": {
    "parameters": [
      {
        "name": "input",
        "default": "",
        "description": "Output of a `git diff` command."
      }
    ]
  }
}
